{
  "name": "clean-node-api",
  "version": "1.0.0",
  "description": "Teste de Q.A.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "lint-staged": {
    "*.js": [
      "standard --fix",
      "git add"
    ]
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "husky": "^8.0.3",
    "jest": "^29.5.0",
    "standard": "^17.0.0"
  },
  "dependencies": {
    "lint-staged": "^13.2.2"
  }
}
